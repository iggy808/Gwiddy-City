using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OldManDialogueLines : MonoBehaviour
{

    /*
     Hopefully this will simply be an array of dialogue lines that we can just pull from this file and 
     import it to the Dialogue.cs file whenever the player interacts with the old man
     
     
     */

    // This contains the lines in the Inspector!
    public string[] OldManIntroLines;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
